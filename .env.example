# ============================================================
# HubSpot ↔ AWS Partner Central Sync — Environment Variables
# Copy to .env for local development. Never commit .env to git.
# ============================================================

# ----------------------------------------------------------
# HubSpot
# ----------------------------------------------------------

# Private App access token from HubSpot Developer Portal
# Settings > Integrations > Private Apps > Your App > Auth
HUBSPOT_ACCESS_TOKEN=pat-na1-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Webhook signing secret (from HubSpot Webhook settings)
# Used to verify incoming webhook requests are genuine
HUBSPOT_WEBHOOK_SECRET=your-hubspot-webhook-secret

# ----------------------------------------------------------
# AWS
# ----------------------------------------------------------

# The region where Partner Central API calls are made
AWS_REGION=us-east-1

# Full ARN of the HubSpotPartnerCentralServiceRole
# Leave blank to auto-construct from the current AWS account
# Format: arn:aws:iam::<ACCOUNT_ID>:role/HubSpotPartnerCentralServiceRole
PARTNER_CENTRAL_ROLE_ARN=

# AWS credentials for local development (not needed in Lambda)
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_SESSION_TOKEN=

# ----------------------------------------------------------
# Application
# ----------------------------------------------------------
ENVIRONMENT=development
LOG_LEVEL=INFO

# How often to poll for Partner Central invitations (minutes)
# This is an EventBridge schedule — set in template.yaml for Lambda
INVITATION_POLL_INTERVAL_MINUTES=5
